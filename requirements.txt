# Web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
jinja2>=3.1.2

# Configuration
python-dotenv>=1.0.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Image processing
Pillow>=10.0.0
# HEIF/HEIC decoder for Pillow. Required to strip EXIF (incl. GPS) from
# iPhone uploads - without it those photos cannot be sanitized and are refused.
pillow-heif>=0.15.0

# AI Providers
google-genai==2.19.0  # pinned: per-part media_resolution needs v1alpha; unpinned builds drifted 2.16-2.19

# OAuth & Google APIs
authlib>=1.3.0
itsdangerous>=2.1.0
httpx>=0.25.0
google-api-python-client>=2.100.0
google-auth>=2.23.0
cachetools>=5.3.0

# Database
sqlalchemy>=2.0.0
alembic>=1.13.0
psycopg2-binary>=2.9.0

# Production server
gunicorn>=21.0.0
