# Authentication
# Set to true to disable all authentication (local development)
AUTH_DISABLED=false

# Google OAuth (required when AUTH_DISABLED=false)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Session secret (required for production - generate with: openssl rand -hex 32)
SESSION_SECRET_KEY=your-secret-key-here

# Frontend URL for OAuth redirects (only needed for split frontend/backend deployment)
# FRONTEND_URL=https://omj-validator.onrender.com

# Access Control - choose one option:

# Option 1: Email allowlist (simplest - comma-separated list of allowed emails)
ALLOWED_EMAILS=user1@gmail.com,user2@example.com

# Option 2: Google Groups API (requires Workspace Admin + Domain-Wide Delegation)
# GOOGLE_GROUP_EMAIL=your-group@googlegroups.com
# GOOGLE_SERVICE_ACCOUNT_JSON={"type":"service_account",...}

# AI Provider
AI_PROVIDER=gemini

# Gemini API Configuration
GEMINI_API_KEY=your-gemini-api-key-here
GEMINI_MODEL=gemini-3-pro-preview
GEMINI_TIMEOUT=90

# App Configuration
UPLOAD_MAX_SIZE_MB=10

# Database (PostgreSQL)
# Local development uses Docker on port 5433 by default
# DATABASE_URL=postgresql://omj:omj@localhost:5433/omj

# Data Directory (optional, for cloud deployments)
# DATA_DIR=/data
